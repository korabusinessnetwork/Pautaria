# ─────────────────────────────────────────────────────────────────────────────
# Reescrita de SPA. Lido por Cloudflare Pages e por Netlify, mesma sintaxe.
#
# O 200 é o que diferencia reescrita de redirecionamento: a URL na barra do
# navegador continua /quadro/abc, e o React Router resolve a rota. Um 301 aqui
# jogaria o usuário para / e ele perderia o link que abriu.
#
# Não precisa da exclusão de /assets/ que o vercel.json tinha: nos dois hosts o
# arquivo estático existente ganha da regra, então o bundle é servido como
# bundle e só o que não existe em disco cai no index.html.
# ─────────────────────────────────────────────────────────────────────────────
/*    /index.html   200
